<div ng-app="UlResourceHub" ng-controller="GamesController" ng-init="init()"> <h1>Games</h1>
  <%= link_to 'New Game', new_game_path, class: "btn btn-default" %>
  <br/>
  <!-- <ul class="pagination">
    <li><a ng-click="setPage(currentPage - 1)">&laquo;</a></li>
    <li><a ng-click="setPage(currentPage + 1)">&raquo;</a></li> 
  </ul> -->
<div ar-paging></div>

<table class="table"> <thead>
<tr> <th>Name</th>
      </tr>
    </thead>
<tbody>
<tr ng-repeat="game in games">
<td>{{ game.name }}</td> </tr>
    </tbody>
  </table>
</div>
